<% flash.each do |message_type, message_content| %>
  <div class="ui message transition <%= message_type %>">
    <i class="close icon"></i>
    <div class="header">
      <%= message_content %>
    </div>
  </div>
<% end %>